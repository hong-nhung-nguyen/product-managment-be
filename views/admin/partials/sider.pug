.sider
    .inner-menu 
        ul 
            li 
                a(href=`${prefixAdmin}/dashboard`) Dashboard

            if(role.permissions.includes("products-category_view"))
                li 
                    a(href=`${prefixAdmin}/products`) Product List
            
            if(role.permissions.includes("products-category_view"))
                li 
                    a(href=`${prefixAdmin}/products-category`) Product Categories
            if(role.permissions.includes("roles_view"))
                li 
                    a(href=`${prefixAdmin}/roles`) Role Groups
            
            if(role.permissions.includes("permissions_view"))
                li 
                    a(href=`${prefixAdmin}/roles/permissions`) Permissions
            
            if(role.permissions.includes("accounts_view"))
                li 
                    a(href=`${prefixAdmin}/accounts`) Account List
            li 
                a(href=`${prefixAdmin}/setting/general`) General Settings

