extends ../../layouts/default.pug 
include ../../mixins/box-head.pug 
include ../../mixins/alert.pug

block main 
    +alert-success(5000)
    +alert-error(5000)

    .container.my-3 
        .row 
            .col-12 
                +box-head("Checkout")
        
        .row 
            .col-12 
                if(cart && cart.products.length > 0)

                    table(class="table table-bordered")
                        thead 
                            tr 
                                th STT 
                                th Image 
                                th Name 
                                th Price 
                                th Quantity 
                                th Total
                        
                        tbody 
                            each product, index in cart.products
                                tr 
                                    td #{index + 1}
                                    td 
                                        img(src=product.productInfo.thumbnail, alt=product.productInfo.slug width="80px")
                                    td #{product.productInfo.title}
                                    td $#{product.productInfo.priceNew}
                                    td #{product.quantity} 
                                    td $#{product.totalPrice}
                                    
                    
                    h3(class="text-end") Order Total: $#{cart.totalPrice}


                else 
                    h1(class="text-center") No products in the shopping cart
        
        if(cart.products.length > 0) 
            .row 
                .col-12 
                    .card.p-4.my-4 
                        form(
                            action=`/checkout/order`
                            method="POST"
                        )
                            div(class="form-group mb-3")
                                label(for="fullName") Full Name
                                input(
                                    type="text"
                                    class="form-control"
                                    id="fullName"
                                    name="fullName"
                                    required
                                )
                            div(class="form-group mb-3")
                                label(for="phone") Phone Number
                                input(
                                    type="text"
                                    class="form-control"
                                    id="phone"
                                    name="phone"
                                    required
                                )
                            div(class="form-group mb-3")
                                label(for="address") Address
                                input(
                                    type="text"
                                    class="form-control"
                                    id="address"
                                    name="address"
                                    required
                                )
                            div(class="form-group")
                                button(
                                    type="submit"
                                    class="btn btn-success btn-block"
                                ) PLACE ORDER
        

    script(src="/js/cart.js")
            
            
